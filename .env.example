# PostgreSQL connection string
# Railway provides this automatically when you add a Postgres plugin
# and link it to this service (see README for exact steps).
DATABASE_URL="postgresql://user:password@localhost:5432/gymtracker"
